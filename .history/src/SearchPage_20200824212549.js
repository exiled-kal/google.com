import React from 'react';
import './SearchPagefirebase.auth().currentUser.sendEmailVerification()
    .then(function () {
        // Email sent.
    }).catch(function (error) {
        // An error happened.
    });'

function SearchPage() {
  return <div className="searchPage"></div>;
}

export default SearchPage;
